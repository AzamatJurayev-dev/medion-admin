export const useBanner = () => {
  //   const updateMutation = useMutation({
  //     mutationFn: updateCategory,
  //     onSuccess: () => refetch(),
  //   });

  return {
    // updateMutation,
  };
};
